The power set of a [[set]] $S$ is the set of all [[subset]]s of $S$, including the [[empty set]] and $S$ itself. It is variously denoted as $\mathcal{P}(S)$ or $2^S$.

The order of a power set of a set of order $n$ is $2^n$.
